Use 'screen mongod' to connect MongoDB
Use these commands to init the database and check the connection: 'node scripts/trymongo.js', 'mongo issuetracker scripts/init.mongo.js'
'npm start' to start the server
